<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DiffusionView.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DiffusionView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid x:Name="AppTitleBar" Height="48" VerticalAlignment="Top" Canvas.ZIndex="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="LeftPaddingColumn" Width="0"/>
                <ColumnDefinition x:Name="IconColumn" Width="Auto"/>
                <ColumnDefinition x:Name="TitleColumn" Width="Auto"/>
                <ColumnDefinition x:Name="RightPaddingColumn" Width="*"/>
            </Grid.ColumnDefinitions>

            <FontIcon Grid.Column="1" FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE91B;" FontSize="16" Margin="8,0,8,0"/>
            <TextBlock Grid.Column="2" Text="Diffusion View" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
        </Grid>

        <NavigationView PaneDisplayMode="Auto" IsBackButtonVisible="Collapsed" IsSettingsVisible="True" Margin="0,48,0,0">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="Home" Tag="home"/>
                <NavigationViewItem Icon="Pictures" Content="Albums" Tag="albums"/>
                <NavigationViewItem Icon="Library" Content="Library" Tag="library"/>
                <NavigationViewItem Icon="Save" Content="Archive" Tag="archive"/>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>
